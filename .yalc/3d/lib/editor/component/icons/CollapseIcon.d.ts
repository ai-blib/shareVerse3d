import { CSSProperties } from "preact/compat";
type CollapseIconProps = {
    style?: CSSProperties;
    onClick?: () => void;
};
declare const CollapseIcon: ({ style, onClick }: CollapseIconProps) => import("preact/compat").JSX.Element;
export default CollapseIcon;
