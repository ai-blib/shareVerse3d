import { CSSProperties } from "preact/compat";
type MetricProps = {
    index: number;
    style: CSSProperties;
};
declare const _default: ({ index, style }: MetricProps) => import("preact/compat").JSX.Element;
export default _default;
